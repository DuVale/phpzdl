;; Jaime Blasco jaime.blasco@alienvault.com
;;
;; GlastopfNG Honeypot

[DEFAULT]
plugin_id=1667

[config]
enable=yes
type=detector

source=log
location=/var/log/GlastopfNG.log

# create log file if it does not exists,
# otherwise stop processing this plugin
create_file=true

process=GlastopfNG.jar
start=no ; launch plugin process when agent starts
stop=no ; shutdown plugin process when agent stops
startup=

[1 - GlastopfNG - Attack Detected]
#'192.168.1.131'|'/data.php?sql=isnert%20into%20caca;unionasdasd'|'19'
event_type=event
regexp='(?P<src_ip>[^']+)'\|'(?P<data>[^']+)'\|'(?P<impact>\d+)'
plugin_sid=1
userdata1={$data}
userdata2={$impact}
src_ip={$src_ip}

